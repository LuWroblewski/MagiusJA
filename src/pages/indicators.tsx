import { Menu } from '../components/Menu';
import { MenuQuestion } from '../components/MenuQuestion';

const Home = () => {
  return (
    <div className='Home'>
      <Menu />
      <MenuQuestion />
    </div>
  );
};

export default Home;
