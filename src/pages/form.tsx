import { Menu } from '../components/Menu';
import { Form } from '../components/form';

const Home = () => {
  return (
    <div className='Home'>
      <Menu />
      <Form />
    </div>
  );
};

export default Home;
