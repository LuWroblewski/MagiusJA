import { Menu } from '../components/Menu';
import { Form } from '../components/form';

const Home = () => {
  return (
    <div className='Home'>
      <Form />
    </div>
  );
};

export default Home;
